extends layout

block content
  header.w3-container.w3-teal
    h1= nome

  h3 Filmes

  if filmes.length > 0
    ul
      each filme in filmes
        li
          a(href="/filmes/" + filme.id)= filme.title